# ROADMAP.md — AI Keeper Director

> Ціль: найкращий мобільний директор атмосфери для ведучих настільних ігор.
> Принцип: один Milestone → один огляд → одне покращення → далі.

---

## ✅ Milestone 1 — Фундамент (готово)
- Єдине джерело правди `getSoundIdByName` (усунуто дублювання в 3 файлах).
- Базова документація: ARCHITECTURE, DECISIONS, ROADMAP, TASKS.

## ✅ Milestone 2 — Локалізація контенту (готово)
- Локалізовані назви/описи сцен і звуків (UA + RU + EN) у `lib/contentI18n.js`.
- Хелпери локалізації підключені у всіх компонентах відображення.

## ✅ Milestone 3 — Розділення домену + шар сховища (готово)
- Розділено `SoundFile` і `SoundButton` (`lib/domain.js`).
- Ізольований шар сховища `lib/storage.js` + хук `useFavorites`.
- Персист гучності майстра (бонус із M5).

## ✅ Milestone 4 — Режими Gameplay / Edit (готово)
- Глобальний стан режиму `lib/ModeContext.jsx` (Play / Edit), стартує в Play.
- Перемикач `ModeToggle` у шапці; підказка в режимі Edit.
- Режим «Підготовка»: тап по звуку відкриває редактор метаданих (гучність, нотатки, verified).
- Шар overrides у `storage` + хук `useSoundOverrides` — каталог SOUNDS незмінний.
- У Play звук грає з користувацькою базовою гучністю; позначка verified на кнопці.
- Не зроблено навмисно: жести (довгий тап / drag) — перенесено в беклог M5.

## ✅ Milestone 5 — Продуктивність і надійність (готово)
- Селекторна підписка `useIsSoundActive` — кнопка перемальовується лише при зміні СВОГО статусу.
- Стабільні екшени `useAudioActions` (без підписки на стан).
- Відновлення AudioContext при поверненні з фону (`useAudioResume`).
- Персист гучності майстра й обраного — зроблено в M3/M4.

## Беклог (з M4/M5)
- Жести: довгий тап / drag-перевпорядкування.

## Майбутнє
- Підтримка систем: D&D, Delta Green, Vaesen, Alien RPG, Pathfinder, Vampire, кастомні.
- Офлайн-кеш (service worker), редактор сцен, автоматизація таймлайну.